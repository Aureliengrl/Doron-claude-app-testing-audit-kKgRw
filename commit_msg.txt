feat: social network - 10 nouvelles fonctionnalites implementees

S3: indicateur de presence en ligne (point vert) sur les tiles d amis
    + statut "En ligne / Vu il y a X" dans le header du chat 1-to-1

S4: long-press sur les messages ouvre un menu copier/supprimer
    (supprimer visible uniquement pour l expediteur)

S5: _sendTypedMessage (partage produit/wishlist) incremente maintenant
    unreadCount pour les participants comme les messages texte

S6: l indicateur "qui tape" affiche le vrai prenom de la personne
    au lieu de "Quelqu un ecrit..."

S8: affichage "Vu X" dans les groupes sous le checkmark de lecture

S9: statut de presence (En ligne / Vu il y a X) affiche sur les profils
    publics des amis

S11: avatar et nom dans le header du chat cliquables -> profil public
     de l interlocuteur

S14: chat vide: CTA avec icone main + "Dites bonjour a X!" au lieu
     de juste "Aucun message"

S15: perf - remplace FutureBuilder par cache (map) pour les noms
     des expediteurs dans les messages de groupe (evite N appels
     Firestore a chaque scroll)

S1+S2: Cloud Functions pour les push notifications
  - sendChatNotification: notifie les destinataires a chaque nouveau
    message (texte + product_card + wishlist_card)
  - sendFriendRequestNotification: notifie lors d une demande d ami
  - Deploy: firebase deploy --only functions (depuis la racine)
